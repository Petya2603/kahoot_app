import 'package:flutter/material.dart';

class ReitingScreen extends StatelessWidget {
  const ReitingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Reiting"),
      ),
      body: const Center(
        child: Text("Reiting"),
      ),
    );
  }
}
